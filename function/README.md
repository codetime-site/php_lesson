# Lesson Functions

## Introductins
 -  [Что ткое Анонимная Функция](#анонимная-Функция)
 - Что такой строгий типизация?
 - [примеры с функциями](#примеры-с-функциями)! 
 - [что такой Стрелочные функции?](#cтрелочные-функции)
 - [Интерфейсы и абстрактные методы](#Интерфейсы-и-абстрактные-методы)
 - [Задачки от Чат GPT](#exampleChatGpt)

---

# Анонимная Функция

```php 

    $name = "Иван";

    $num = 0;
    // анонимный функция
    $sayHello = function() use ($name, &$num) {
        $num++;
        return "Привет, " . $name . " ". $num . " раз. \n"; 
    };

    echo $sayHello();

```
[примеры](func_0.php)

---
<!-- <> -->
# Cтрелочные функции
    
```php
    // Фигурные скобки НЕ нужны
    $double = fn($x) => $x * 2; 

    echo $double(5); // Выведет: 10

```
<!-- </> -->
---

# Интерфейсы и абстрактные методы
 - для чего нужен Интерфейсы и абстрактные методы ?
    - Интерфейсы и абстрактные методы нужны для создания строгих правил (контрактов) в коде. Они заставляют программиста обязательно написать определенные методы в дочерних классах, гарантируя, что программа не сломается из-за отсутствия нужной функции.
 - Их главные задачи:
    - Унификация кода: Все разработчики пишут методы с одинаковыми названиями.
    - Полиморфизм: Позволяют подменять одни объекты другими (например, заменить отправку SMS на Email), не меняя остальной код.- - - 
    - Безопасность: Защищают от архитектурных ошибок на этапе написания кода.

 - При объявлении методов в интерфейсах или абстрактных методов в классах фигурные скобки не ставятся, так как у них нет тела. Вместо них ставится точка с запятой ;:
```php
interface MyInterface {
    public function test(); // Без фигурных скобок
}


```
---


## exampleChatGpt
 - [примеры в коде](example.php) 
 - Задача 1 (★☆☆☆☆) Напишите функцию sum(), которая принимает два числа и возвращает их сумму.

    ```php 
        sum(5, 7);

        Результат:
        12
    ```
    [пример №1](example.php#L8)

    ---

    - Задача 2 (★★☆☆☆) 
      - Напишите функцию isEven(). Она должна вернуть true, если число четное, иначе false.
    
    ```php
        sum(5, 7);

        Результат:
        12 
    ```
    [пример №2](example.php#L18)

    ---

    - Задача 3 (★★★☆☆) 
        - Напишите функцию findMax(). Принимает массив чисел и возвращает максимальное значение.
    
```php
    [10,3,55,8]

    Ответ:
    55
```

    Нельзя использовать max().
    [пример №3](example.php#L27)
    ---


    - Задача 4 (★★★★☆) Создайте функцию calculator().
    ```php
        calculator(5,3,"+")
        calculator(5,3,"-")
        calculator(5,3,"*")
        calculator(5,3,"/")
    ```
    [пример №3](example.php#L35)

    Функция должна сама определить операцию.
    
    ---

    - Задача 5 (★★★★★) Создайте функцию generatePassword().
    Аргументы:
    - длина
    - использовать цифры
    - использовать буквы
    - использовать спецсимволы
    
    Пример
    ```php 
        generatePassword(16,true,true,true)
    ```

Вернуть случайный пароль.

# примеры с функциями.

 * функции можно обявить в переменный 
    
 ```php 
    function number(int $a) :int {
        return  $a = $a*5;
    }

    // фуекции можно обявить в переменный 
    $testVar = number(22);
    echo $testVar + $testVar;

    // output:220

 ``` 

 * вызвать в функции, функцию

```php

    function number(int $a) :int {
        return  $a = $a*5;
    }

    function test ( $n ){
        return number($n);
    }

    echo test( 5 );
    echo "\n"; 

// output:25

````


